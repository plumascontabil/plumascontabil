@model Demonstrativo.Models.OfxDescricao

@{
    Layout = "_LayoutSemFiltroEmpresa";
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>OfxDescricao</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="Descricao" class="control-label"></label>
                <input asp-for="Descricao" class="form-control" />
                <span asp-validation-for="Descricao" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ContaDebitoId" class="control-label"></label>
                <select asp-for="ContaDebitoId" class="form-control" asp-items="ViewBag.ContaDebitoId"></select>
                <span asp-validation-for="ContaDebitoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ContaCreditoId" class="control-label"></label>
                <select asp-for="ContaCreditoId" class="form-control" asp-items="ViewBag.ContaCreditoId"></select>
                <span asp-validation-for="ContaCreditoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
